// Optional LLMS generation script placeholder (CommonJS).
// Keeps cross-platform builds stable when package.json specifies "type": "module".
try {
  // no-op for now
} catch (err) {
  // swallow errors to avoid breaking builds
}

// export nothing
